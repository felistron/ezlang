sum: (a, b) {
    return + a b;
}

inc: (a) {
    return sum(1, a);
}

sub: (a, b) {
    return - a b;
}

double: (a) {
    return * a 2;
}

main: () {
    num1 = double(+ 16 1);
    num2 = double(+ 1 16);
    resultado = sum(num1, num2);
    resultado = inc(resultado);
    return sub(69, resultado);
}